# Добавить пользователя в группу (username -> vboxusers)
sudo usermod -aG vboxusers username

# Поиск справки по слову "password"
man -k password

# Создание софт ссылки
ln file sameFile

# Создание хард сссылки 
ln -s file sameFile